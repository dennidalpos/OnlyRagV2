{
  "todos": [
    "Riscrivere il Workspace Explorer come albero unico (Files e Storico come nodi espandibili dello stesso albero per il progetto attivo, invece delle 3 tab separate Files/Progetti/Storico)",
    "Completare lo split di agentOrchestratorAppService.ts (1266 righe): il while-loop di runAgentOrchestratorLoop resta una singola funzione stateful con ~15 variabili mutabili condivise (stepCount, sessionNumCtx, hasFileMutations, ecc.) — gli altri 6 file (AgentActionLogPanel.tsx, CodingAgentView.tsx, PlanPanel.tsx, SkillHubModal.tsx, SlmDiagnosticsPanel.tsx, WorkspaceExplorer.tsx) sono stati scomposti in moduli <220 righe; per questo file sono stati estratti solo i pezzi puri/autonomi (update_plan handler in agentOrchestratorPlanTool.ts, setup pre-loop in agentOrchestratorSessionSetup.ts) perché spezzare il loop stesso richiederebbe passare lo stato mutabile tra file, sostituendo un rischio con uno peggiore senza un vero refactor architetturale (es. reducer/state machine esplicita)",
    "Spostare le chiamate dirette a fs/child_process fuori dal layer Application (agentOrchestratorAppService.ts, agentToolExecutorService.ts, ollamaAppService.ts, sidecarAppService.ts, skillAppService.ts, systemAppService.ts, workspaceAppService.ts) in repository dell'Infrastructure layer",
    "Unificare .assistant/ e .onlyrag/ in un'unica cartella .onlyrag/ con sottocartelle (assistant/, sessions/), aggiornando tutti i file che referenziano i path letterali e .gitignore",
    "Virtualizzare la timeline messaggi in AgentActionLogPanel.tsx e sostituire l'euristica a caratteri del banner di contesto con un conteggio token reale",
    "Aggiungere checkpoint/rollback per-step delle azioni dell'agente (snapshot prima di ogni modifica per poter annullare l'ultimo step senza usare git manualmente)",
    "Aggiungere approvazione per-hunk nel PendingApprovalModal invece dell'attuale tutto-o-niente sull'intera modifica proposta",
    "Aggiungere ricerca cross-progetto nello storico prompt, una volta introdotto il modello dati multi-progetto del Workspace Explorer"
  ]
}
